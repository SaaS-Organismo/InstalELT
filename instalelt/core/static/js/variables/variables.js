let lastWireId = 0;
let lastTerminalId = 0;
let terminals = []
let wires = [];
let mouseX = 0;
let mouseY = 0;
let mousePosition = {
    x: mouseX,
    y: mouseY
};

export default (lastTerminalId, lastWireId, terminals, wires, mousePosition);